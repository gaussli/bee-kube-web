---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-04-16T18:20:03.192Z
provider: 
---

# Commit Command (/cc)

## Trigger
用户输入 `/cc` 时触发此规则。

## Purpose
一键完成：stage 所有变更文件 → 分析 diff → 生成符合规范的 commit message → 提交

## Workflow

### Step 1: Stage 所有变更
```bash
git add -A
git diff --cached --stat
```

### Step 2: 分析变更内容
获取 staged 的 diff 内容，分析：
- 新增文件 (new file)
- 修改文件 (modified file)
- 删除文件 (deleted file)

### Step 3: 生成 commit message
遵循以下规范：
1. **使用英文描述**
2. **多个要点用 `;` 分隔**
3. **格式**: `type: description`
   - `feat`: 新功能
   - `fix`: 修复 bug
   - `update`: 更新/优化
   - `refactor`: 重构
   - `style`: 样式调整
   - `docs`: 文档
   - `chore`: 构建/工具

### Step 4: 提交
```bash
git commit -m "commit message"
```

## Example
输入: `/cc`
输出:
```
feat: add user detail drawer with BeeTab; create BeeTab component with sliding indicator; add user avatar component
```

## Constraints
- 如果没有变更文件，不执行提交
- commit message 保持简洁，每个要点不超过 50 字符
- 优先使用 `feat`, `fix`, `update`, `refactor`, `style`, `chore` 作为 type